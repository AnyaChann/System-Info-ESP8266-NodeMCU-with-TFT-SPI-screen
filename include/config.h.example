// Config Example - Copy file này thành config.h và chỉnh sửa
// Hoặc lấy giá trị từ .env ở thư mục gốc

#ifndef CONFIG_H
#define CONFIG_H

// ===== Debug Configuration =====
// Uncomment để enable debug logs
// #define DEBUG_MODE       // Master debug switch - enable all debug
// #define DEBUG_BUTTON     // Enable button debug logs (click counting, etc)
// #define DEBUG_NETWORK    // Enable network debug logs
// #define DEBUG_OTA        // Enable OTA debug logs

// Debug Helper Macros
#ifdef DEBUG_MODE
  #define DEBUG_PRINT(x) Serial.print(x)
  #define DEBUG_PRINTLN(x) Serial.println(x)
  #define DEBUG_PRINTF(fmt, ...) Serial.printf(fmt, ##__VA_ARGS__)
#else
  #define DEBUG_PRINT(x)
  #define DEBUG_PRINTLN(x)
  #define DEBUG_PRINTF(fmt, ...)
#endif

// OTA Configuration
#define OTA_ENABLED true              // Enable/disable OTA updates
#define OTA_HOSTNAME "ESP8266-OTA" // Hostname for OTA
#define OTA_PASSWORD ""               // OTA password (empty = no password)

// ===== TFT Display Configuration =====
// Choose ONE display type (uncomment the one you have):

// ===== Supported Displays (with Adafruit libraries) =====
// ST7735 - Small displays (1.44" - 1.8")
#define TFT_ST7735    // Default: 1.8" 160x128 or 128x160
// #define ST7735_INITR INITR_BLACKTAB   // Black tab (default)
// #define ST7735_INITR INITR_REDTAB     // Red tab  
// #define ST7735_INITR INITR_GREENTAB   // Green tab
// #define ST7735_INITR INITR_144GREENTAB // 1.44" 128x128 Green tab

// ST7789 - Medium displays (1.3" - 2.4")
// #define TFT_ST7789    // 1.3" 240x240, 1.54" 240x240, 2.0" 240x320, 2.4" 240x320

// ILI9341 - Popular medium size (2.2" - 2.8")
// #define TFT_ILI9341   // 2.2" 240x320, 2.4" 240x320, 2.8" 240x320

// ===== Screen Dimensions (Optional) =====
// Uncomment and set for your specific screen (auto-detected if not set)
// Common sizes:
// 1.44" ST7735:    128x128 (INITR_144GREENTAB)
// 1.8" ST7735:     128x160 or 160x128
// 1.3" ST7789:     240x240
// 2.0" ST7789:     240x320
// 2.4" ST7789:     240x320
// 2.2" ILI9341:    240x320
// 2.4" ILI9341:    240x320
// 2.8" ILI9341:    240x320

// #define TFT_WIDTH  128
// #define TFT_HEIGHT 160

// Note: For other displays (GC9A01, ILI9486, ST7796), you need to:
// 1. Install the appropriate library manually
// 2. Update display_manager.h and display_manager.cpp
// 3. Or use TFT_eSPI universal library instead

// Chân kết nối TFT (SPI) - Theo sơ đồ đấu nối của bạn
#define TFT_CS    D8   // GPIO 15
#define TFT_RST   D4   // GPIO 2
#define TFT_DC    D3   // GPIO 0
#define TFT_LED   D2   // GPIO 4 - Backlight control (PWM)
// MOSI (SDA) → D7 (GPIO 13) - Hardware SPI
// SCK        → D5 (GPIO 14) - Hardware SPI

// Button Configuration (bật/tắt màn hình)
#define BUTTON_PIN D1  // GPIO 5 (nối GND để bấm)

// Display Settings
#define SCREEN_ROTATION 0  // 0-3 (xoay màn hình 0°, 90°, 180°, 270°)
#define BACKLIGHT_TIMEOUT 60000  // Tự tắt sau 60s không hoạt động (ms)

// ===== Refresh Rate Configuration =====
// Tần suất cập nhật dữ liệu từ server (milliseconds)
// Lower value = faster updates but more network/CPU usage
// Recommended values:
// 33ms  = ~30 FPS (Rất nhanh - có thể lag do HTTP request)
// 100ms = ~10 FPS (Mượt mà - khuyến nghị cho màn hình nhỏ)
// 200ms = ~5 FPS  (Cân bằng tốt)
// 500ms = ~2 FPS  (Tiết kiệm - mặc định cũ)
// 1000ms = 1 FPS  (Chậm nhưng ổn định)
#define REFRESH_INTERVAL 5000  // Default: 500ms

#endif // CONFIG_H